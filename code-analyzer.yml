rules:
  pmd:
    # This AppExchange heuristic treats ordinary business fields containing
    # words such as Key, Assigned, Access, and Token as credentials. Actual
    # secrets are forbidden from package metadata and live in Named Credentials.
    ProtectSensitiveData:
      disabled: true

engines:
  flow:
    disable_engine: true
