<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Eligible_Amount__c</fullName>
    <description>Eligible funding ceiling computed at submission from the
  tenant's industry-scoped MDF_Calculation_Rule__mdt configuration. Null if
  no rule matched - informational only, doesn't block submission; a
  reviewer approving above this amount needs the same MDF budget override
  authority as exceeding the account's annual budget.</description>
    <label>Eligible Amount</label>
    <precision>16</precision>
    <required>false</required>
    <scale>2</scale>
    <type>Currency</type>
</CustomField>
